apiVersion: v1
kind: Namespace
metadata:
  name: timing-experiment
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: timing-pod-ubuntu-8
  namespace: timing-experiment
spec:
  serviceName: "timing-service"
  replicas: 8
  selector:
    matchLabels:
      app: timing-experiment
      image-type: ubuntu
      pod-count: "8"
  template:
    metadata:
      labels:
        app: timing-experiment
        image-type: ubuntu
        pod-count: "8"
      annotations:
        core-id: "8-15"
    spec:
      restartPolicy: Always
      containers:
      - name: timing-container
        image: timing-experiment:ubuntu
        imagePullPolicy: Never
        volumeMounts:
        - name: shared-storage
          mountPath: /shared
      volumes:
      - name: shared-storage
        hostPath:
          path: /mnt/experiment-data/ubuntu-8pods
          type: DirectoryOrCreate
